params:
  NAME: 
platform: linux
image_resource:
  type: docker-image
  source:
    repository: ubuntu
inputs:
  - name: source-code
run:
  path: sh
  args:
    - -c
    - |
      echo "Hello $NAME. Today is $(date)"
      env
